import java.sql.SQLOutput;
import java.util.HashMap;

public class Counting {

    public static void main(String[] args) {
        String bodyOfText;
        HashMap<Character, Integer> results = null;

        bodyOfText = "If the product of two terms is zero then common sense says at least one of the two terms has to be zero to start with. So if you move all the terms over to one side, you can put the quadratics into a form that can be factored allowing that side of the equation to equal zero. Once you’ve done that, it’s pretty straightforward from there.";

char[] chars = bodyOfText.toCharArray();
//loop through the chars array to add to results key
        System.out.println(results);
        //then search for key in bodyOfText and add count of key as value
    }
}
